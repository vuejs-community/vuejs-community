<div align="center">

<img src="public/logo.svg" alt="Vue Community logo" width="160" />

# Vue Community

**Discover the Vue Ecosystem** — an open, community-driven directory of the Vue ecosystem.

</div>

## ✨ Features

- 🗂 **3,500+ projects indexed** — the largest curated collection of the Vue ecosystem, spanning 8 categories: UI libraries, components, composables, Nuxt modules, build-tool plugins, starters, admin templates, and the uni-app ecosystem
- 📊 **Live ecosystem stats** — npm downloads and GitHub stars synced automatically every day by a scheduled GitHub Action, so you always see fresh numbers
- 🔍 **Rich filtering & sorting** — query by category, source, stars, or monthly / weekly downloads, with sorting built into the API
- ⚡ **Fast, data-driven API** — all data is compiled into a prebuilt SQLite index served by Nitro, no runtime database required
- 🤖 **Auto-generated data** — Nuxt modules are generated from `nuxt/modules`, and Vite / Rollup / Rolldown / Unplugin plugins are discovered from npm keywords, keeping the catalog wide and up to date
- 🛡 **Type-safe by design** — every entry is validated against `@vuejs-community/schema`, so the data stays consistent
- 🎨 **Modern UI** — built with Tailwind CSS v4, shadcn-vue (reka-ui), and smooth animations via motion-v
- 🌱 **Community-driven** — adding a project is as simple as one small TypeScript file, no database writes needed

## 🛠 Development

Clone and run the project locally:

```bash
# 1. Clone the project
git clone https://github.com/vuejs-community/vuejs-community.git

# 2. Enter the project directory
cd vuejs-community

# 3. Install dependencies
pnpm install

# 4. Start the dev server on http://localhost:3000
pnpm run docs:dev
```

## 📁 Project Structure

```
.
├── app/                         # Nuxt frontend
│   ├── assets/                  # Global styles and the custom @nuxt/icon collection
│   ├── components/              # Site features and reusable UI components
│   │   └── ui/                  # Base components generated by shadcn-vue
│   ├── composables/             # Project data-fetching and metadata composables
│   ├── layouts/                 # Shared page layouts
│   └── pages/                   # File-based routes
├── server/                      # Nitro backend
│   ├── api/                     # Project list, metadata, and statistics endpoints
│   ├── plugins/                 # Runtime database setup
│   └── assets/                  # Generated SQLite index (index.db)
├── shared/                      # Types, constants, and utilities shared by app and server
├── modules/
│   └── llms/                    # Local Nuxt module that serves /llms.txt
├── packages/                    # pnpm workspace packages
│   ├── data-ui/                 # Curated UI library entries
│   ├── data-component/          # Curated component library entries
│   ├── data-hooks/              # Curated composable / hooks entries
│   ├── data-admin/              # Curated admin dashboard entries
│   ├── data-uniapp/             # Curated uni-app ecosystem entries
│   ├── data-nuxt/               # Auto-generated Nuxt module entries
│   ├── data-plugins/            # Auto-generated build-tool plugin entries
│   ├── generate-data/           # Data collectors and generators
│   ├── schema/                  # Catalog types and defineProjectMeta helper
│   ├── shared/                  # Reusable npm and GitHub data utilities
│   ├── sync/                    # npm download and GitHub star synchronization
│   └── tsconfig/                # Shared TypeScript configuration
├── scripts/                     # SQLite index generation and validation
├── public/                      # Static assets served as-is
├── nuxt.config.ts               # Nuxt application configuration
├── pnpm-workspace.yaml          # Workspace package configuration
└── turbo.json                   # Turborepo task configuration
```

## 🔄 Data Pipeline

Project metadata lives in `packages/data-*` as one TypeScript file per project. The pipeline regenerates the automated catalogs, compiles every catalog into `server/assets/index.db`, refreshes its npm and GitHub metrics, and then validates the result:

| Command | Purpose |
| --- | --- |
| `pnpm run generate:plugins` | 🔌 Regenerate build-tool plugin entries from npm |
| `pnpm run generate:nuxt:modules` | 🧩 Regenerate Nuxt module entries from `nuxt/modules` |
| `pnpm run generate:db` | 🗄 Compile all `data-*` packages into the SQLite index while preserving existing metrics |
| `pnpm run sync` | 📥 Refresh npm downloads and GitHub stars in the SQLite index (`GENERATE_TOKEN` required) |
| `pnpm run validate:db` | ✅ Verify the generated database schema and records |

A scheduled GitHub Action runs these commands in order every day, so the catalog and its statistics stay current automatically.

## 🤝 Contributing

Contributions are welcome! Whether you want to **add or fix a data entry**, **improve the site UI**, or **report an issue**, please read the [Contributing Guide](./CONTRIBUTING.md) first — it covers environment setup, project structure, common commands, and commit conventions.

## 📄 License

[MIT](./LICENSE)
